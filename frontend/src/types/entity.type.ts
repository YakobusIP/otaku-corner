type GenreEntity = {
  id: number;
  name: string;
};

type StudioEntity = {
  id: number;
  name: string;
};

type ThemeEntity = {
  id: number;
  name: string;
};

export type { GenreEntity, StudioEntity, ThemeEntity };
